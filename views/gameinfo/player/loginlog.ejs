<% title = 'Лог входа / Панель игровой информации' + " — " + __('global.pagetitle'),
	backUrl = '/gameinfo';  %>
<%- partial('../partials/header')%>


<%- partial('../partials/subheader', { subheader: 'Игрок / Лог входа' }) %>
<div class="container" style="margin-top: 20px">
	<form method="GET">
	<div class="ui small form">
		<div class="three fields">
			<div class="field">
				<label>Ник игрока</label>
				<div class="ui fluid input">
					<input placeholder="ChangedNick" type="text" name="nickname" id="gc-formnicknameinput" value="<%= (req.query.nickname) ? req.query.nickname : '' %>">
				</div>
			</div>
			<div class="field">
				<label>IP</label>
				<div class="ui fluid input">
					<input placeholder="89.250.6.68" type="text" name="ip" id="gc-formipinput" value="<%= (req.query.ip) ? req.query.ip: '' %>">
				</div>
			</div>
			<div class="field">
				<label>HWID</label>
				<div class="ui fluid input">
					<input placeholder="f6d972a7def04ca8d625c4bb098816ab" type="text" name="hwid" id="gc-formhwidinput" value="<%= (req.query.hwid) ? req.query.hwid : '' %>">
				</div>
			</div>
		</div>
		<div class="ui blue submit button" id="gc-formfirstsubmit">Submit</div>
	</div>
	</form>
	<div class="ui divider"></div>
	<% if (log && log.length) { %>
	<table class="ui basic small table">
		<thead>
			<tr>
				<th>Логин</th>
				<th>Время</th>
				<th>IP</th>
				<th>HWID</th>
				<th>Статус</th>
			</tr>
		</thead>
		<tbody>
			<% log.forEach(function (element) { %>
			<tr>
				<td><a href="/gameinfo/player/loginlog?nickname=<%= element.login %>"><%= element.login %></a> <small><i><a href="/gameinfo/player/info?nickname=<%= element.login %>">info</a></i></small></td>
				<td><%= moment(element.time).format('YYYY-MM-DD h:mm:ss') %></td>
				<td><a href="/gameinfo/player/loginlog?ip=<%= element.ip %>"><%= element.ip %></a></td>
				<td><a href="/gameinfo/player/loginlog?hwid=<%= element.hwid %>"><%= element.hwid %></a></td>
				<td><%= element.status %></td>
			</tr>
			<% }) %>
		</tbody>
	</table>
	<% } else if (log && !log.length) { %>
	<div style="padding: 5em 0em;text-align: center;font-size:1.4em">Похоже, что игрок никогда не заходил</div>
	<% } else { %>
	<div style="padding: 5em 0em;text-align: center;font-size:1.4em">Введите ник игрока</div>
	<% } %>
</div>
